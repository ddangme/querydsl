spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: ddangme
    password: qwerty123456

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true # 실행되는 JPQL를 보도록 설정 /* ~~ */

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace